import re
n, m = map(int, input().split())
matrix = [input() for _ in range(n)]
matrix = list(zip(*matrix))
decoded_script = "".join(["".join(col) for col in matrix])
print(re.sub(r'(?<=[a-zA-Z0-9])[^a-zA-Z0-9]+(?=[a-zA-Z0-9])', ' ', decoded_script))
